post_install() {
    echo
    echo ">>> If you would like to run Sketch without logging "
    echo ">>> out and back in again, run the command"
    echo ">>> source /etc/profile.d/sketch-synth.sh"
    echo
}
